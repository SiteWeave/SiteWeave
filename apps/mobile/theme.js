/**
 * Shared design tokens for SiteWeave mobile (monday-led field app).
 * Sourced from @siteweave/design-tokens so web/mobile share semantic roles.
 * See MOBILE-DESIGN.md. Org primary/secondary can override via useBranding().
 */

export {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  touch,
  motion,
  createTheme,
  defaultTheme,
} from '@siteweave/design-tokens/mobile';
